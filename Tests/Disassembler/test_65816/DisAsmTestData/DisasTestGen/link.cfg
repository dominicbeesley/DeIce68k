MEMORY {
    ZP:  start = $0020, size = $0020, type = rw, define = yes;
    RAM: start = $1000, size = $4000, file = %O, fill=no;
    FRAM: start = $120000, size = $1000;
}
SEGMENTS {
    CODE:     load = RAM, type = ro;
    ZEROPAGE: load = ZP,  type = zp;
    DATA:     load = RAM, type = rw;
    FDATA:    load = FRAM, type = rw;
}
FEATURES {
}
SYMBOLS {
}
